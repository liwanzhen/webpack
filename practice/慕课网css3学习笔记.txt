111 浏览器前缀
-webkit       chrome safari
-moz          firefix
-ms           IE
-o            opera    


222 文字超出的时候显示省略号
text-overflow:ellipsis;
overflow:hidden;
white-space:nowrap;
width:100px;

文本换行：word-break:break-all;(不兼容)
          word-wrap:break-word;


333.box-shadow:1px 1px 1px #ccc inset;
    text-shadow:1px 1px 1px #ccc;


444.属性选择器

a[class^=icon] {}    类名以icon开头的a标签
a[class$=icon] {}    类名以icon结束的a标签
a[class*=icon] {}    类名包含icon的a标签

:root  是给整个文档加样式
input:not([type="submit"]) {}
div:empty {border: 1px solid green;}
p:first-child {}      指定只有当<p>元素(必须)是其父级的第一个子级的样式。
p:first-of-child {}   选择每个p元素是其父级的第一个p元素
p:nth-child(2) {}     选择每个p元素是其父级的第二个p元素
p:nth-of-type(2) {}   选择每个p元素是其父级的第二个p元素

::selection {}  鼠标选中时的样式（不兼容火狐需加前缀-moz）

:enabled :disabled :checked :selection :read-only :read-write（非只读状态时的样式）

.clearfix::before,
.clearfix::after {
    content: ".";
    display: block;
    height: 0;
    visibility: hidden;
}
.clearfix:after {clear: both;}
.clearfix {zoom: 1;}

http://www.imooc.com/code/1882  用css3实现点击图片切换背景图片
